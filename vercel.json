{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "installCommand": "npm install --legacy-peer-deps",
          "startCommand": "npm run start",
          "buildCommand": "npm run generate-prisma && npm run build"
        }
      }
    ]
  }