{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node"
      }
    ],
    "build": {
      "env": {
        "PRISMA_GENERATE": "true"
      },
      "commands": {
        "build": "npm run prisma-generate && next build"
      }
    }
  }
  